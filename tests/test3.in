//Multiply test

int main(void) {
    int x,y;
    if (x != y) {
        int x;
        x = 42 + y + 3 + 6 + 8; // zatim se dava 1 vzdy
    } else {
        y = 42 + x; // zatim se dava 1 vzdy
    }
    int y;
    return 0;
}